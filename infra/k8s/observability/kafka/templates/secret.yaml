apiVersion: v1
kind: Secret
metadata:
  name: kafka-cluster-id
  namespace: observability
type: Opaque
stringData:
  # Generate with: kafka-storage.sh random-uuid
  # or: python3 -c "import uuid; print(str(uuid.uuid4()))"
  CLUSTER_ID: ""
