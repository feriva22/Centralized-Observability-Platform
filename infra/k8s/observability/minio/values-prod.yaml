minio:
  replicaCount: 4

  persistence:
    enabled: true
    size: 200Gi
    storageClass: "fast-ssd"

  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi
