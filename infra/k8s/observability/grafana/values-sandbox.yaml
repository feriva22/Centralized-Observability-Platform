environment: sandbox

grafana:
  replicas: 1

  persistence:
    enabled: false

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  grafana.ini:
    auth.anonymous:
      enabled: true
      org_role: Viewer
